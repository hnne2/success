<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.CAMERA" />

    <uses-feature
        android:name="android.hardware.camera"
        android:required="false" />

    <application
        android:name=".App"
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@drawable/ikonsaccesh"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme"
        tools:targetApi="31">

        <activity
            android:name=".activities.qrcodeActiviti"
            android:exported="false" />
        <activity
            android:name=".activities.test"
            android:exported="false" />
        <activity
            android:name=".activities.FirstStartL"
            android:exported="false" />
        <activity
            android:name=".activities.InHotell.InHotel"
            android:exported="false"
                />
        <activity
            android:name=".activities.Zaseleniyee.Zaseleniye"
            android:exported="false" />
        <activity
            android:name=".activities.registr.regestration"
            android:exported="false" />
        <activity
            android:name=".activities.Avtorizationn.avtorization"
            android:exported="false" />
        <activity
            android:name=".activities.MainActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <meta-data
            android:name="preloaded_fonts"
            android:resource="@array/preloaded_fonts" />
    </application>

</manifest>